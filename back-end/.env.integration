# Environment preset for local Prisma + PostgreSQL + HeidiSQL workflow
# Credentials: postgres/postgres on localhost:5432 using database agendunas
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/agendunas?schema=public"

# Allow the default local front-end dev servers
ALLOWED_ORIGINS="http://localhost:5173,http://localhost:3000"

# HTTP port exposed by the back-end dev server
PORT=3001
